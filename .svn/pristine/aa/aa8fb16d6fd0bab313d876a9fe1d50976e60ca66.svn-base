package com.dwms.course.dao;

import com.dwms.common.config.MyMapper;
import com.dwms.course.domain.CourseChapter;

public interface CourseChapterMapper extends MyMapper<CourseChapter> {
    
    //计算总的章节数（没有节的章和所有节）
    int getChapterNum(Integer courseId);
}